/* function Cliente (nombre, saldo){
    this.nombre=nombre;
    this.saldo=saldo;
    this.depositar=depositar;
    this.extraer=extraer;
}

function depositar(dinero){
    this.saldo = this.saldo+dinero;
    console.log( this.saldo);
}
function extraer(dinero){
    if(dinero>this.saldo){
        console.log( 'no hay suficiente saldo');
    }else{
    this.saldo = this.saldo-dinero;
console.log('tu saldo es ' +this.saldo);}
}
let cliente1 = new Cliente('alberto', 23);
cliente1.depositar(100);
cliente1.extraer(200); */

/* const btn=document.getElementById('btn').addEventListener('click', cambiarColor);
const contenedor= document.getElementById('contenedor');
function cambiarColor(){
    if(contenedor.style.background==='black'){
   contenedor.style.background='red';}
    else{
        contenedor.style.background='black';}

    }
 */